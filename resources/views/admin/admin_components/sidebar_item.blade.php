<li class="nav-item ">
    <a class="nav-link" href="{{ url($url) }}" ><i class="{{ $icon_class }}"></i>{{ $title }} @if($show_badge) <span class="badge {{ $badge_class }}">{{ $badge_value }}</span>@endif</a>
</li>

